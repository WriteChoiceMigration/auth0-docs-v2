---
title: "Explorer les déclencheurs"
permalink: "explore-triggers"
'og:title': "Explorer les déclencheurs"
'og:description': "En savoir plus sur les déclencheurs, qui sont des événements qui déclenchent automatiquement une Action lorsqu’une opération spécifique, telle que l’ouverture d’une session par un utilisateur, se produit au moment de l’exécution."
'og:image': "https://cdn2.auth0.com/docs/1.14567.0/img/share-image.png"
'twitter:title': "Explorer les déclencheurs"
'twitter:description': "En savoir plus sur les déclencheurs, qui sont des événements qui déclenchent automatiquement une Action lorsqu’une opération spécifique, telle que l’ouverture d’une session par un utilisateur, se produit au moment de l’exécution."
---
# Explorer les déclencheurs

Les déclencheurs sont des points dans le processus d’Auth0 où des actions peuvent être ajoutées pour répondre aux demandes de personnalisation. Plusieurs actions, exécutées les unes après les autres, peuvent être associées à un déclencheur. Les déclencheurs peuvent être synchrones (bloquant) ou asynchrones (non bloquant).
Chaque déclencheur dispose d’objets et d’API spécifiques. Ils peuvent varier selon votre mode de connexion (par ex. sans mot de passe).

Pour explorer les objets de référence et les plans pour des Actions spécifiques pour un déclencheur, sélectionnez le déclencheur ci-dessous, en notant la définition Auth0 d’une [connexion sans mot de passe](/docs/fr-ca/authenticate/passwordless).

#### Inscription et connexion

Lorsqu’un utilisateur est ajouté à une base de données ou une connexion sans mot de passe (Inscription), ou dans le cadre d’un processus d’authentification (Connexion).

<table class="table"><thead>
<tr>
<th>Déclencheur</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>pre-user-registration</code></td>
<td>Se déclenche avant que l’utilisateur soit créé.</td>
</tr>
<tr>
<td><code>post-user-registration</code></td>
<td>Se déclenche de manière asynchrone après la création d’un utilisateur.</td>
</tr>
<tr>
<td><code>post-login</code></td>
<td>Se déclenche après l’authentification de l’utilisateur, mais avant l’émission d’un jeton.</td>
</tr>
</tbody>
</table>

#### Notifications MFA

Lorsque vous utilisez le SMS comme l’un des authentification multifacteur (<Tooltip data-tooltip-id="react-containers-DefinitionTooltip-0" tip="">MFA</Tooltip>) pour vous authentifier ou pour configurer un fournisseur personnalisé.

<table class="table"><thead>
<tr>
<th>Déclencheur</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>send-phone-message</code></td>
<td>Se déclenche lorsqu’un fournisseur personnalisé est utilisé pour envoyer les messages liés à l’inscription et au processus du défi-réponse.</td>
</tr>
</tbody>
</table>

#### Réinitialisation du mot de passe

Lorsque la réinitialisation du mot de passe est lancée.

<table class="table"><thead>
<tr>
<th>Déclencheur</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>post-change-password</code></td>
<td>Se déclenche après la modification du mot de passe.</td>
</tr>
<tr>
<td><code>password-reset-post-challenge</code></td>
<td>Se déclenche après le premier défi-réponse et avant la réinitialisation du mot de passe.</td>
</tr>
</tbody>
</table>

#### machine-machine

Lorsqu’une application, et non un utilisateur, est authentifiée et autorisée à utiliser vos API.

<table class="table"><thead>
<tr>
<th>Déclencheur</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>credentials-exchange</code></td>
<td>Se déclenche avant qu’un jeton d’accès soit émis en retour.</td>
</tr>
</tbody>
</table>